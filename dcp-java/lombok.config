# Lombok configuration — see https://projectlombok.org/features/configuration
config.stopBubbling = true

# @Jacksonized: Spring Boot 4 uses Jackson 3 (tools.jackson.*).
# Property is a list — use += (Lombok 1.18.44+).
lombok.jacksonized.jacksonVersion += 3
